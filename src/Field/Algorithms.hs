{-# LANGUAGE UnicodeSyntax #-}
module Field.Algorithms where

-- | Функция поиска свободных позиций в строке
-- findFreePos ∷ BSC8.ByteString → [Int]
-- findFreePos = (\xs → if xs == [] then id  else (map fst )). filter ((== ' ').snd) . zip [0..]
